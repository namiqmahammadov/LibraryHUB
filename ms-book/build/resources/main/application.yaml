spring:
  data:
   redis:
     host: localhost
     port: 6379
  cache:
    type: redis

  rabbitmq:
    host: localhost
    port: 5672
    username: namiq
    password: 12345
  application:
    name: ms-book
  datasource:
    url: jdbc:postgresql://localhost:5433/books_db
    username: postgres
    password: passsword
  jpa:
      open-in-view: false
      properties:
        hibernate:
          format_sql: true
          show_sql: true
      hibernate:
        ddl-auto: update
server:
  port: 8082
  springdoc:
    api-docs:
       path: /v3/api-docs
  swagger-ui:
      path: /swagger-ui.html



